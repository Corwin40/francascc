{% if sections %}
{% for section in sections %}
    <div class="container">
        <h3>{{ section.name }}</h3>

    </div>

{%  endfor %}
{% else %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h3>Pas de contenu</h3>
            </div>
        </div>
    </div>
{% endif %}