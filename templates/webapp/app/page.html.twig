{% extends 'base.html.twig' %}

{% block title %}Op - Accueil{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-12">
            <h1>{{ page.title }}</h1>
        </div>
    </div>
</div>
    {{ render(controller('App\\Controller\\Webapp\\SectionController::ListAllSections', {page: page.id})) }}
{% endblock %}
